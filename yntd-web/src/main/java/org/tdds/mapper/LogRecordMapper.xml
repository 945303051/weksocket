<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tdds.mapper.LogRecordMapper">
	<!-- 单表查询映射结果 -->
    <sql id="Base_Column_List">
    </sql>
    <select id="findRunningData" resultType="java.lang.Double">
		SELECT
			IFNULL(TIMESTAMPDIFF(SECOND,MIN(record_time),MAX(record_time)),0)  AS timed
		FROM znzz_running_record
		WHERE
		 <choose>
		 	<when test="map.date!=null and map.date!=''">
		 		 DATE(record_time) =#{map.date}
		 	</when>
		 	<otherwise>
		 		DATE(record_time) =CURRENT_DATE
		 	</otherwise>
		 </choose>
		 <if test="map.machineId!=null and map.machineId!=''">
		  AND machine_id =#{id}
		 </if>
    </select>
    <select id="exportPowerOffData" resultType="map">
    	select
    	record_time as recordTime
    	from
    	znzz_poweroff_record
    	where
    	record_time between startTime  and endTime; 
    	and
    	machineId=#{id}; 
    </select>
    <select id="exportAlarmData" resultType="map">
    </select>
      <select id="exportWaittingData" resultType="map">
    </select>
</mapper>
